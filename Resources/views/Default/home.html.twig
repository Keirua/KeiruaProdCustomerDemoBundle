{% extends '::base.html.twig' %}{% block body %}Affichage <br />{% for customer in customers %}	<a href="{{ path('KeiruaProdApplicationBundle_show', {'id': customer.id}) }}">{{ customer.name}}</a> <br />	{% for prod in customer.products %}		- {{ prod.name }} <br />	{% endfor %}{% endfor %}<a href="{{ path('KeiruaProdApplicationBundle_add') }}">Ajouter un client</a>{% endblock %}